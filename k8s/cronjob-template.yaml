apiVersion: batch/v1
kind: CronJob
metadata:
  name: claude-pipeline-{REPO_SLUG}
  namespace: claude-pipeline
  labels:
    app.kubernetes.io/name: claude-pipeline
    app.kubernetes.io/component: agent
    claude-pipeline/repo: "{REPO_SLUG}"
spec:
  schedule: "{SCHEDULE}"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: {JOB_DEADLINE}
      ttlSecondsAfterFinished: 3600
      template:
        metadata:
          labels:
            claude-pipeline/repo: "{REPO_SLUG}"
        spec:
          serviceAccountName: claude-pipeline-agent
          restartPolicy: Never
          volumes:
            - name: cargo-registry-cache
              persistentVolumeClaim:
                claimName: cargo-registry-cache
          containers:
            - name: agent
              image: {IMAGE}
              imagePullPolicy: {IMAGE_PULL_POLICY}
              resources:
                requests:
                  memory: "2Gi"
                  cpu: "1000m"
                limits:
                  memory: "8Gi"
                  cpu: "4000m"
              volumeMounts:
                - name: cargo-registry-cache
                  mountPath: /home/pipeline/.cargo/registry
              env:
                - name: REPO_URL
                  value: "{REPO_URL}"
                - name: ANTHROPIC_MODEL
                  value: "{MODEL}"
                - name: CLAUDE_MODEL
                  value: "{MODEL}"
                - name: ANTHROPIC_BASE_URL
                  value: "{ANTHROPIC_BASE_URL}"
                - name: GIT_AUTHOR_NAME
                  value: "{GIT_AUTHOR_NAME}"
                - name: GIT_AUTHOR_EMAIL
                  value: "{GIT_AUTHOR_EMAIL}"
                - name: ANTHROPIC_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: claude-pipeline-secrets
                      key: ANTHROPIC_API_KEY
                - name: ANTHROPIC_AUTH_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: claude-pipeline-secrets
                      key: ANTHROPIC_API_KEY
                - name: GIT_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: claude-pipeline-secrets
                      key: GIT_TOKEN
                - name: GITHUB_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: claude-pipeline-secrets
                      key: GIT_TOKEN
                - name: GH_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: claude-pipeline-secrets
                      key: GIT_TOKEN
